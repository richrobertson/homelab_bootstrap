
machine:
  logging:
    destinations:
      - endpoint: "udp://203.0.113.5:5045/"
        format: "json_lines" 
      - endpoint: "tcp://203.0.113.5:5044/"
        format: "json_lines"  
      
  install:
    extraKernelArgs:
      - "cpufreq.default_governor=performance"
      - "intel_idle.max_cstate=0"
      - talos.logging.kernel=tcp://203.0.113.5:5044/